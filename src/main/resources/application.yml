spring:
  datasource:
    url: jdbc:postgresql://localhost/coolplanet
    username: david
    password: password
  flyway:
    enabled: true
    url: jdbc:postgresql://localhost/coolplanet
    user: david
    password: password
---
spring:
  datasource:
    url: jdbc:postgresql://db/coolplanet
  flyway:
    url: jdbc:postgresql://db/coolplanet
