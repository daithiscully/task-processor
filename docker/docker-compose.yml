version: '3.1'

services:

  db:
    image: postgres:14.2
    restart: always
    environment:
      - POSTGRES_USER=david
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=coolplanet
    ports:
      - "5432:5432"
    volumes:
      - ./db-data/:/var/lib/postgresql/data/
